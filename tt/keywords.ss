(library (tt keywords)
  (export
    forall
    class type typed lambda pi quote
    typeof
    boolean number char string
    -> =>
    datum source)
  (import (syntax))

  (define-keywords
    forall
    class type typed lambda pi quote
    typeof
    boolean number char string
    -> =>
    datum source)
)
