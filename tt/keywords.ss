(library (tt keywords)
  (export
    forall class type typed lambda pi quote
    typeof
    boolean number char string
    -> =>
    datum
    unchecked
    and or)
  (import (syntax))

  (define-keywords
    forall class type typed lambda pi quote
    typeof
    boolean number char string
    -> =>
    datum
    unchecked
    and or)
)
