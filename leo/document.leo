library
  leo document
  export: document, example
  import leo scheme

  define-syntax
    when
      document: x, ...
      begin: write `x, ...
    when
      example: x, ...
      `example
        program: x, ...
        result` run: x, ...
