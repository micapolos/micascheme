library
  leo document
  export: document, example
  import leo scheme

  define-macro
    when
      document: x, ...
      begin: write `x, ...
    when
      example: x, ...
      `example
        program: x, ...
        result` run: x, ...
