(library (leo maker)
  (export maker)
  (import
    (scheme)
    (identifier)
    (leo lookup))

  (define-syntax (maker $syntax)
    (lambda (lookup?)
      (syntax-case $syntax ()
        ((_ id)
          (safe-lookup? lookup? #'id #'maker)
          (lookup? #'id #'maker))
        ((_ id)
          (identifier-append #'id #'make #'- #'id)))))
)
