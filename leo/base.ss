library
  leo base
  export
    let
    let-values
    let*
    let*-values
    letrec
    letrec*
  import rnrs base

  export
    except
      let
      let-values
      let*
      let*-values
      letrec
      letrec*
      from import rnrs base
